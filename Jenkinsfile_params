pipeline {
    agent any
   parameters {
        string(name: 'NAME')
        string(name: 'TAG')
        string(name: 'HASH')
    }
    stages {
        stage('User Input') {
            steps {
                script {
                    echo "Введённые версии"
                }
            }
        }
    }
}

